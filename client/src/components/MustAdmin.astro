<script>
    import { pb, refreshAuth } from "../pocketbase";

    if (await refreshAuth()) {
        if (pb.authStore.record?.collectionName != "_superusers") {
            window.location.href = "/admin/login";
        }
    } else {
        window.location.href = "/admin/login";
    }
</script>
